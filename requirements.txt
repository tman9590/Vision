# ── Core inference ───────────────────────────────────────────────────────────
numpy>=1.24,<3.0
opencv-python-headless>=4.8
onnxruntime>=1.16

# ── Training (heavy — install separately if only running inference) ──────────
ultralytics>=8.0

# ── CoreML export & inference (macOS only) ───────────────────────────────────
# coremltools>=7.0      # uncomment on macOS
